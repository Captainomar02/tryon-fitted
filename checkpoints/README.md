# Checkpoints

SAM 3D Body checkpoints are stored under:

```text
checkpoints/sam-3d-body-dinov3/
```

Expected files:

```text
checkpoints/sam-3d-body-dinov3/model.ckpt
checkpoints/sam-3d-body-dinov3/assets/mhr_model.pt
```

Download them with:

```bash
scripts/vast/download_checkpoints.sh
```
